# Photon MPPT Configutation Software

### This code requires google protobuf to be installed

**On ubuntu linux:**
* sudo apt-get update
* udo apt-get install python-protobuf

**On mac with homebrew:**
* brew tap homebrew/versions
* brew install protobuf
